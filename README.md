This is a static portfolio site built using Node.js and the Express web framework.

Custom CSS:
Added custom icons to error page, Centered the text underneath index images, changed border around profile image to grey